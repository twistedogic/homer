{
  "name": "homer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "homer-cli": "./packages/cli/bin/homer-cli.cjs"
  },
  "scripts": {
    "prepare": "vite build --config vite.config.cli.ts",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
